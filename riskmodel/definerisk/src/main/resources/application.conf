# In "application.conf" file


app {
  name = "definerisk"
  version = "1.0"
  features {
    enableLogging = true
    maxConnections = 100
  }
  database {
    url = "jdbc:mysql://localhost:3306/mydb"
    user = "dbuser"
    password = "not used"
  }
  files {
    dataFileLocation = "c:/temp/positions.csv" 
    outputDirectory = "c:/temp/results.dat" 
  }
}
